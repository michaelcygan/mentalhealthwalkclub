/**
 * Hard cap for a Solo Walk timer. Not a fitness limit — prevents a forgotten
 * timer from continuing indefinitely and damaging routine statistics.
 */
export const SOLO_WALK_MAX_SECONDS = 4 * 60 * 60;

export const SOLO_WALK_REFLECTION_PROMPT = "What is worth keeping?";
