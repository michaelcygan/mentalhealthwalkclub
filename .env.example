# Lovable Cloud (Supabase) — client-visible
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""

# Lovable Cloud — server-only (available at runtime via Lovable secret storage)
SUPABASE_URL=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_PROJECT_ID=""
# SUPABASE_SERVICE_ROLE_KEY — injected by Lovable; never commit or paste locally

# Place search — Photon (OpenStreetMap). Server-only override.
# Defaults to https://photon.komoot.io when unset.
PHOTON_BASE_URL=""

# Payments (Stripe) — frozen for V1 launch, kept for future
VITE_PAYMENTS_CLIENT_TOKEN=""

# Guest RSVP encryption (server-only). 32+ random chars, mixed case + symbols.
GUEST_RSVP_ENCRYPTION_KEY=""
